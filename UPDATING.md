# 作品集更新方式

## 新增作品

1. 複製任一 `case-*.html` 作為新案例頁，替換內容與圖片。
2. 將圖片放進 `assets/case-專案名稱/`，避免不同案例的素材混在一起。
3. 在 `portfolio-projects.js` 新增一筆作品資料，並放到想要的排序位置。

首頁卡片、案例編號、案例數量與每頁底部的下一篇會依 `portfolio-projects.js` 自動更新，不必逐頁改連結。

## 更新既有作品

- 文字與畫面敘事：修改對應的 `case-*.html`。
- 首頁摘要、標籤、縮圖或作品順序：修改 `portfolio-projects.js`。
- 全站樣式：修改 `styles.css`。
- 全站互動與首次載入動畫：修改 `script.js`。

## 上線前檢查

- 案例頁內的圖片與說明是否對應。
- 客戶名稱、站名、座標與內部資料是否完成去識別化。
- 桌機、平板與手機是否沒有文字重疊或橫向捲動。
- 首頁關鍵字點選後是否能篩出相關案例。
